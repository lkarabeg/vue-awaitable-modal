import { ModalContext } from "@/vue-awaitable-modal/modalContext"
import { ModalService, ModalProps, modalServiceInstance } from "@/vue-awaitable-modal/modalService"
import ModalRoot from "@/vue-awaitable-modal/ModalRoot.vue"

export default modalServiceInstance

export {
    ModalContext,
    ModalRoot,
    ModalService
}

export type {
    ModalProps
}
